import XCTest
@testable import Rookery

final class RookeryTests: XCTestCase {
    func testScaffoldBuilds() throws {
        // Placeholder so `swift test` has something to run on a fresh checkout.
        // Real tests land with v0.1 features (cron parser, SFTP path resolver, etc.).
        XCTAssertTrue(true)
    }
}
